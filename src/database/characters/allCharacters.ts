import { CCharacter } from "src/classes/c-character";
import charSeele from "./seele";

let allCharacters: CCharacter[] = [];

// Добавляем персонажей в массив
allCharacters.push(charSeele);

export default allCharacters;
